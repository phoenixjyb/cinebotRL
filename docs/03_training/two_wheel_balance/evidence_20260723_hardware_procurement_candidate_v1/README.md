# Riser Hardware Procurement Candidate V1

This CPU-only evidence distinguishes a single instrumented 400 W engineering
sample from a production-ready riser selection.

- `summary.json` is generated by
  `scripts/two_wheel_balance/audit_riser_hardware_procurement_candidate.py`.
- Vendor facts are pinned in
  `docs/03_training/two_wheel_balance/RISER_VENDOR_SPEC_SNAPSHOT_20260723.json`.
- The 400 W candidate passes the nominal 8 kg calculation with only a 1.0557
  emergency torque margin, below the new 1.15 production requirement.
- One non-riding bench sample is recommended; production procurement, hardware
  transfer, training, and GPU work remain false.

The audit changes no robot asset, trajectory, controller, dynamic gate, runtime
authorization, residual label, or learned policy.
