# Two-wheel riser end-goal completion audit

This evidence was generated by
`scripts/two_wheel_balance/audit_riser_goal_completion.py` from clean,
synchronized commit `d83fa50f29761f967d204dfeadd7090631f535e4`.

The machine-readable result is `summary.json`, SHA-256:

`3b00bad293b061a323d97ced6010bb0313423421b7deaa089202d29dfcdd8c6d`

The report is byte-identical when generated with macOS Python and the
authoritative Windows IsaacLab Python on `.98`.

## Result

- Required gates passed: `6/10`.
- The isolated worktree, arm-free robot asset, frozen LQR baseline, simulated
  `0.60--1.80 m` and `1 m/s` riser baseline, exact-source 79-case reference,
  and 750 W motor/mechanism design recommendation pass.
- The missing required gates are the real `4+2` corrective training corpus,
  authorized projection-aware BC policy, learned-policy all-79 dynamic gate,
  and learned-policy render audit.
- Physical 750 W bench qualification remains false. It is tracked separately
  from the requested design recommendation and must not be presented as
  production hardware approval.
- Obstacle avoidance remains outside this goal.
- The audit started no runtime, capture, BC, PPO, or learned rollout.

The goal is not achieved.
