# Case-32 Validation Selection

This CPU-only amendment replaces retired validation case 16 with case 32.
Case 8 remains unchanged. Historical case-32 evidence is used only to select
and prepare a fresh paired canary; it is not a current runtime or dataset
admission.

All runtime, capture, conversion, merge, BC, PPO, and training fields remain
false.
