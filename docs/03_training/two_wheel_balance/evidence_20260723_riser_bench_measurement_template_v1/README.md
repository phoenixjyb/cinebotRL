# Riser Bench Measurement Template Audit V1

This CPU-only evidence proves that the unmeasured bench template fails closed.

- `summary.json` is generated by
  `scripts/two_wheel_balance/audit_riser_bench_measurements.py`.
- The template intentionally contains no measured numeric values or evidence
  hashes.
- The audit reports 34 missing or invalid measurement fields and recommends
  collecting complete calibrated bench evidence.
- Production procurement, hardware transfer, runtime, GPU work, training, BC,
  and PPO remain false.

The project thresholds are engineering review gates, not vendor ratings or a
safety certification. A future measured pass permits production design review
only; it does not grant automatic procurement or hardware-transfer approval.
