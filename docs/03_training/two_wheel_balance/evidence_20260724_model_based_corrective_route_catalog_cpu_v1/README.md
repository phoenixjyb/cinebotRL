# Model-based corrective route catalog CPU checkpoint

This checkpoint consolidates the repeated CPU preparation for the pending
case-7 train, case-8 validation, and case-16 validation routes into one
fail-closed catalog and one preflight command.

The catalog binds 61 route identities and the canonical teacher contract:
65 observations, three bounded residual actions, action scales
`[0.05, 0.05, 0.02]`, and previous effective post-supervisor action history.
The existing case-specific wrappers remain the runtime backends, so this
change does not alter plans, controller commands, dynamic gates, corrective
profiles, or safety ownership.

macOS and `.98` Windows Python generated byte-identical reports at SHA-256
`74aafa819003e41fbf8040ad4ba563915a149ae21e4fb55b98d20c5567ddbd52`.
The focused compatibility suite passed `46 passed, 2 warnings in 6.96s`
locally and `46 passed, 2 warnings in 66.63s` on `.98`.

Implementation commit
`b5292df1ebf704f13e7e67552502b70635c34197` is pushed and synchronized.
No authorization token, runtime namespace, Isaac/GPU workload, capture,
conversion, corpus merge, BC, PPO, checkpoint, or training run was created.
The next data-producing operation remains separately authorized case 7.
