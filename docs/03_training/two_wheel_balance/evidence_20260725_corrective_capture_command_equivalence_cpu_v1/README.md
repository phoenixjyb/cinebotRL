# Corrective-capture command equivalence audit v1

This CPU-only checkpoint audits the four model-based corrective capture
wrappers at implementation commit
`bce0f1ba2a4df99ef321290ab33391e68953f72a`.

The auditor normalizes explicit wrapper options against the pinned playback
defaults, verifies every controller and dynamic-gate value, binds plan,
corrective-profile, wrench-profile, gains, playback, namespace, and output
paths to each route contract, and rejects unknown runtime options.

Cases 6, 23-v4, and pending case 7 are current-command-compatible. Historical
case 30 is deliberately not reusable as a current launch template: it binds an
older playback identity and omits the now-required explicit capture split.
Its existing evidence remains untouched.

macOS and Windows Python generated byte-identical reports at SHA-256
`6a7de89133a5b6177f6d42e7558f272b611481f231d0494f05263b605c4b9bd2`.
No generic runtime wrapper, authorization token, Isaac/GPU process, capture,
conversion, corpus merge, BC, PPO, checkpoint, or training run was created.

The next data-producing operation remains separately authorized:
`Authorize exactly one case-7 corrective-label capture.`
