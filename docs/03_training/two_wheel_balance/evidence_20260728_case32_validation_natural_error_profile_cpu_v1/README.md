# Case-32 Structural Validation Profile

The profile retains `31.720785%` of case 32's observed raw residual envelope,
bounded by its own unchanged p95 dynamic margin. It uses natural tracking
error and forbids external perturbation.

The proposal is CPU-ready for a future hash-bound paired route only. No
runtime route, authorization token, label capture, dataset, BC, PPO, or
training was created.
